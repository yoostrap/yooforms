import { admin as required } from './required';
import { admin as maxDate } from './maxdate';
import { admin as minDate } from './mindate';
import { admin as maxNumber } from './maxnumber';
import { admin as minNumber } from './minnumber';
import { admin as maxItems } from './maxitems';
import { admin as minItems } from './minitems';
import { admin as maxLength } from './maxlength';
import { admin as minLength } from './minlength';

export default [
    required,
    minDate,
    maxDate,
    minNumber,
    maxNumber,
    minItems,
    maxItems,
    minLength,
    maxLength,
];
